# GlitchTip — self-hosted error tracking (Sentry-compatible).
#
# psm service: uses the shared psm postgres. Start with `psm up glitchtip`, which
# starts postgres if needed, creates the `glitchtip` database and injects
# PSM_POSTGRES_URL. Every variable, with its default, lives in .env.schema.
# Secrets ship with an insecure dev-only default — override them for anything shared.
name: glitchtip

x-psm:
  depends_on:
    postgres:
      database: glitchtip

services:
  valkey:
    image: ${VALKEY_IMAGE}:${VALKEY_TAG}
    restart: unless-stopped

  web:
    image: ${GLITCHTIP_IMAGE}:${GLITCHTIP_TAG}
    depends_on:
      - valkey
    ports:
      - "${GLITCHTIP_PORT}:8000"
    environment:
      DATABASE_URL: ${PSM_POSTGRES_URL}
      VALKEY_URL: ${GLITCHTIP_VALKEY_URL}
      SECRET_KEY: ${GLITCHTIP_SECRET_KEY}
      EMAIL_URL: ${GLITCHTIP_EMAIL_URL}
      GLITCHTIP_DOMAIN: ${GLITCHTIP_DOMAIN}
      DEFAULT_FROM_EMAIL: ${GLITCHTIP_DEFAULT_FROM_EMAIL}
      ENABLE_ADMIN: ${GLITCHTIP_ENABLE_ADMIN}
      ENABLE_OPENAPI: ${GLITCHTIP_ENABLE_OPENAPI}
      GLITCHTIP_ENABLE_MCP: ${GLITCHTIP_ENABLE_MCP}
      GLITCHTIP_ENABLE_DUCKDB: ${GLITCHTIP_ENABLE_DUCKDB}
      SERVER_ROLE: all_in_one
    restart: unless-stopped
    volumes:
      # :U chowns the host dir to the image's non-root `app` user (rootless podman)
      - ${PSM_VOLUMES_HOME}/glitchtip/uploads:/code/uploads:U
