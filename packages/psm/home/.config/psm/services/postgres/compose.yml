---
version: '3.8'

services:
  postgres:
    image: postgres:16-alpine
    container_name: postgres
    ports:
      - "5432:5432"
    volumes:
      - ~/.local/share/psm/volumes/postgres_data:/var/lib/postgresql/data:Z

